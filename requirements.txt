# Prefect v3 and essential dependencies
prefect>=3.0.0

# Cloud provider SDKs (keep minimal; install only what you use)
# AWS
boto3>=1.28.0

# GCP
google-cloud-run>=0.10.0
google-cloud-logging>=3.5.0

# Azure
azure-identity>=1.14.0
azure-containerregistry>=1.1.0

# Optional: useful for flow development and scripting
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.0.0
