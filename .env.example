# ============================================================================
# LOCAL DEV - Self-Hosted Prefect Server (Default)
# ============================================================================
# Self-hosted Prefect Server runs locally via Docker Compose
# No external cloud account required for local development

# Prefect Server API endpoint (self-hosted in Docker)
# Use container name "prefect-server" for worker, or "localhost" for host CLI
PREFECT_API_URL=http://prefect-server:4200/api

# Not needed for self-hosted in local dev
PREFECT_API_KEY=

# ============================================================================
# OPTIONAL: Prefect Cloud (For Remote Orchestration)
# ============================================================================
# To use Prefect Cloud instead, uncomment below and comment out self-hosted above
# Then run: docker-compose up -d prefect-worker (skip prefect-server)

# PREFECT_API_URL=https://api.prefect.cloud/api
# PREFECT_PROFILE=cloud
# PREFECT_API_KEY=your-prefect-cloud-api-key-here

# ============================================================================
# AWS ECS Deployment
# ============================================================================
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=123456789012
AWS_ECR_REPO=prefect-lab
AWS_ECS_CLUSTER=prefect-cluster
AWS_ECS_TASK_FAMILY=prefect-worker
AWS_SUBNETS=subnet-abc123,subnet-def456
AWS_SECURITY_GROUPS=sg-abc123

# ============================================================================
# GCP Cloud Run Deployment
# ============================================================================
GCP_PROJECT=my-project-id
GCP_REGION=us-central1
GCP_AR_REPO=prefect-lab
GCP_AR_LOCATION=us-central1

# ============================================================================
# Azure Container Apps Deployment
# ============================================================================
AZ_SUBSCRIPTION=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZ_RESOURCE_GROUP=prefect-rg
AZ_LOCATION=eastus
AZ_ACR_NAME=prefectacr
AZ_CONTAINERAPPS_ENV=prefect-env

# ============================================================================
# Logging & Debugging
# ============================================================================
PREFECT_LOGGING_LEVEL=INFO
# PREFECT_LOGGING_LEVEL=DEBUG  # Uncomment for verbose output
